{
  "name": "@mono/monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "watch": "npm start -w",
    "build": "npm run build -w",
    "build-core": "npm run build -w @mono/core"
  },
  "workspaces": [
      "./packages/*"
  ]
}